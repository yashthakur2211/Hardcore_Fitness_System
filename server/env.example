# Database Configuration
# Update these with your MySQL/MariaDB credentials
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=Yash_2211
DB_NAME=gymmaster_db

# JWT Secret (Change this to a random strong secret in production!)
# Generate a random string for better security
JWT_SECRET=dckjsdkjascsduvsvbjsfdf

# Server Configuration
PORT=5000

# Frontend URL (for CORS)
# Update this if your frontend runs on a different port
FRONTEND_URL=http://localhost:5173
